# 开源项目学习总计划（2026.9.17 制定）

> **目标**：通过精读 `open-source-learning/` 下的两个开源项目，补齐生产级后端工程能力 + Agent 应用开发能力，最终产出一个能写进简历、经得起面试追问的完整项目。
>
> **结构**：主线（项目学习，每天 3-4h）+ 支线（校招八股，每天 1h + 碎片时间），双线并行，总周期约 **9 周**。

---

## 〇、现状评估（2026.9.17）

| 技术 | 当前水平 |
|------|---------|
| Python | 基本没问题 |
| FastAPI | 零基础 |
| SQL / 数据库 | 零基础 |
| SQLAlchemy / Alembic / JWT / pytest / Docker | 均未学过 |
| LLM 基础 | 仅听说过概念 |

**结论**：`full-stack-fastapi-template` 的前置技能基本未掌握，需要先花 2 周打地基，不能直接精读模板。原 README 中"4 周学完"的节奏放宽为 9 周。

---

## 一、学习方法论（每个技术点都走三步，禁止跳步）

```
① 跟教程写一遍（15%）→ ② 对照模板读懂（30%）→ ③ 合上代码自己写一遍（55%）
```

- **禁止只看不写**：判断标准是"能否不看答案重新实现"
- **禁止抄完就算完**：跟教程敲完，必须回到模板源码找到对应真实写法，对比差异
- **允许看不懂**：第一遍精读遇到不懂的（如 Alembic 内部机制），标记 `# TODO 理解` 继续，二刷再解决，不要死磕
- **每周日"合卷自测"**：不看资料重新实现本周核心内容，写不出来就在下周补——这是唯一能防止"假学会"的方法

---

## 二、主线：9 周项目学习计划

### 阶段 0：打地基（第 1-2 周）—— 不碰两个项目

> 零基础直接读生产级模板会被 SQLAlchemy 2.0 + 异步 + 依赖注入三座大山劝退，先用最小教程建立基础。

#### 第 1 周：FastAPI 官方教程 + SQL 同步开坑

| 内容 | 资源 | 节奏 |
|------|------|------|
| FastAPI 入门 | FastAPI 中文官方文档 | 每天一小节，重点：路径/查询参数、Pydantic 模型、依赖注入、异步路由 |
| SQL 入门 | sqlbolt.com（或"SQL ZOO"/菜鸟教程 SQL） | 每天一个查询小节，只学 SELECT/WHERE/JOIN/聚合，建库建表后置 |

**第 1 周验收**：独立写一个"图书管理迷你 API"——两张表（用户/书籍），增删改查接口 + 参数校验（先用内存 list 代替数据库）。

#### 第 2 周：异步编程 + SQLAlchemy 2.0 入门

| 内容 | 说明 |
|------|------|
| Python 异步 | 只学 `async/await`、`asyncio.run`、为什么 FastAPI 全是 async；B 站看一个高分入门视频即可，不深入事件循环原理 |
| SQLAlchemy 2.0 | 官方 2.0 教程 ORM 部分，重点三件事：`DeclarativeBase`、`Mapped[]`、`session` |

**第 2 周验收**：给图书 API 接上真 SQLite 数据库，用 SQLAlchemy 完成增删改查。

---

### 阶段 1：精读 full-stack-fastapi-template（第 3-5 周）

> 项目定位：FastAPI 作者 tiangolo 维护的官方生产级全栈模板，价值在于**生产级后端的标准分层**。

**前置动作**：先把项目跑起来（`docker compose up` 或按 `development.md` 本地跑），能打开前端登录页即成功。跑不通就排查——这本身就是学习。

#### 第 3 周：核心链路精读（最重要的一周）

按"一次请求的生命周期"顺序读：

```
main.py（应用入口、CORS、路由挂载）
→ api/routes/login.py（登录接口：密码校验、JWT 签发）
→ api/deps.py（依赖注入：如何从 token 拿到当前用户）★ 全模板最精华的文件
→ models.py（User/Item 表定义）
→ crud.py（数据库操作）
→ api/routes/items.py（标准业务 CRUD 路由）
```

每读完一个文件，合上代码自己重写一遍核心逻辑。

#### 第 4 周：配套技术各个击破（每项 1-2 天，目标只是"看懂模板用法"）

| 技术 | 学到什么程度 | 参考 |
|------|-------------|------|
| JWT 鉴权 | 理解"签发→携带→验证"流程，看懂 `core/security.py` | 搜"JWT 图解" |
| Alembic | 会 `alembic revision --autogenerate` + `upgrade head`，理解迁移的意义 | 官方 tutorial 30 分钟版 |
| pytest | 会写基本接口测试，看懂模板 `tests/` 结构 | 模板自带测试就是最好教材 |
| Docker | 只需 up/down/logs 三个命令 + 大致看懂 `compose.yml` | 不用系统学 |

**前端跳过**：能跑起来就行，一点不用学，定位是后端 + Agent。

#### 第 5 周：改造练习（本阶段产出）

**Fork 模板 → 删除 items 演示模块 → 换成自己的业务**（如"学习打卡"：LearningRecord 表 + CRUD 接口 + pytest 测试）。

**阶段 1 总验收**：不看模板，从空目录搭出一个带 JWT 登录 + 一张业务表 CRUD + 一条 pytest 的最小后端。

---

### 阶段 2：openai-agents-python（第 6-7 周）

> 项目定位：OpenAI 官方 Agent SDK。核心概念：Agent、function_tool、handoffs、Runner、guardrails、tracing、MCP。

#### 第 6 周：Agent 基础概念 + 基础示例

前置补课（2-3 天）：
- LLM API 基础：messages 结构、system prompt、temperature（看 DeepSeek 或 OpenAI 的 API 文档入门页）
- Function Calling 原理：LLM 不是真的执行函数，而是返回"我想调用哪个函数"（Agent 的心脏）

按顺序跑 `examples/`（用 DeepSeek API Key，成本低）：

```
basic/hello_world.py           → Agent = 名字 + 指令 + 模型
basic/function_tool.py         → 工具调用（★最重要）
basic/tools.py
agent_patterns/ 常用模式
```

#### 第 7 周：完整示例精读 + handoffs

```
customer_service/              → 多 Agent 协作（handoff）
financial_research_agent/      → 完整真实项目的组织方式
mcp/ 基础用法                  → 知道即可，不深入
```

**阶段 2 验收**：不看示例，独立写一个"两 Agent 协作"小程序——Agent A 接需求分诊，Agent B 调用工具（查天气/算数/读文件）完成任务。

---

### 阶段 3：产出自己的项目（第 8-9 周）

选题原则：**真实会用 + 展示两个项目的核心技术 + 面试可讲透**。候选（难度递增）：

1. **AI 简历优化助手**：FastAPI 管理用户/简历/优化历史；双 Agent 协作（一个分析简历，一个结合 JD 给建议）+ 工具调用
2. **智能文件整理助手**：Agent 调用文件读写工具自动分类；FastAPI 管理任务与进度
3. **技术调研 Agent**：Agent 自主搜索 + 总结 + 生成 Markdown 报告；FastAPI 管理任务队列和结果

#### 第 8 周：架构重组（不要从零写）

Fork FastAPI 模板，删掉 items 模块，按模板分层加入业务：

```
backend/app/
├── api/routes/
│   ├── agent.py        # 新增：Agent 对话/任务接口
│   └── tasks.py        # 新增：任务管理
├── models.py           # 加自己的表（Conversation、Task）
├── crud.py             # 加对应 CRUD
└── agents/             # 新增目录：Agent 定义（从 examples 改造）
    ├── planner.py
    └── tools.py
```

实现：Agent 对话历史入库、任务接口鉴权、Alembic 加表。

#### 第 9 周：差异化亮点 + 包装

做 1-2 个可量化的亮点（每个都要有对比数据）：

1. **成本/性能对比**：同一任务，单 Agent vs 多 Agent handoff 的 token 消耗和成功率
2. **工程化亮点**：Alembic 管理会话表 + pytest 测 Agent 接口 + 用 tracing 定位一次"Agent 跑偏"的真实案例
3. **模型替换实践**：DeepSeek vs GPT 的效果/成本差异记录

包装动作：
- 推到**自己的 GitHub fork**（不要 push 上游），写高质量 README：架构图（Mermaid）、快速启动、GIF 演示
- 部署在线 Demo（可复用 Cloud Studio 经验），面试现场演示最加分
- 简历写法参考：*基于 FastAPI 官方模板 + OpenAI Agents SDK 独立开发 XX 系统，实现多 Agent 协作/工具调用/会话持久化，含 Alembic 迁移、pytest 测试、Docker 部署*

---

## 三、支线：校招八股计划（每天 1h + 碎片时间，与主线并行）

> 原则：**不需要系统网课**（那是给考研/数据库内核方向的），用面试导向资料效率高 10 倍。
> 顺序有意设计：八股知识在项目里"遇见"过一次，面试时就能答出自己的理解而不是背书。

### 第 1-4 周：《图解 MySQL》（小林 coding，免费）

> 此时主线正好在学 SQL/SQLAlchemy，看索引、事务原理会立刻与实践对上，记忆效率最高。

覆盖校招高频题：
- 索引：B+ 树、聚簇/非聚簇、索引失效、最左前缀
- 事务：ACID、隔离级别、MVCC
- 锁：行锁/表锁/间隙锁、死锁
- 日志：redo log / undo log / binlog
- SQL 优化：explain

配套刷题：牛客"MySQL 练习"或 LeetCode 数据库题 50 道（同时巩固 SQL）。

### 第 5-9 周：《图解网络》（小林 coding，免费）

> 此时主线会遇到 CORS、JWT、HTTP 状态码，同样能互相印证。

覆盖校招高频题：
- TCP/UDP 区别、三次握手/四次挥手（必考中的必考）、TIME_WAIT
- HTTP 1.1/2/3、HTTPS 握手、对称/非对称加密
- 输入 URL 到页面显示发生了什么（经典串联题）
- DNS、ARP、Cookie/Session/Token

### 支线验收

每周末用 30 分钟自测：随机抽 5 个高频题，能脱稿给人讲清楚。

---

## 四、总时间表

| 周次 | 主线（3-4h/天） | 支线（1h/天） |
|------|----------------|---------------|
| 第 1 周 | FastAPI 教程 + SQL 入门 | 《图解 MySQL》 |
| 第 2 周 | 异步 + SQLAlchemy 2.0 | 《图解 MySQL》 |
| 第 3 周 | 模板核心链路精读 | 《图解 MySQL》 |
| 第 4 周 | 配套技术（JWT/Alembic/pytest/Docker） | 《图解 MySQL》 |
| 第 5 周 | 模板改造练习 | 《图解网络》 |
| 第 6 周 | Agents SDK 基础 + basic 示例 | 《图解网络》 |
| 第 7 周 | 完整示例精读 + handoffs | 《图解网络》 |
| 第 8 周 | 项目架构重组 | 《图解网络》 |
| 第 9 周 | 差异化亮点 + README + 部署 | 《图解网络》+ 八股总复习 |

---

## 五、风险提醒

1. **最大风险是贪快**：阶段 0 看似绕路，实际是两个项目所有技术的最小公共前缀，跳过它后面每周都在还债。
2. **SQL 不用学深**：目标是看懂模板、写 CRUD，SELECT/JOIN/外键即可，不要买书系统学数据库原理。
3. **前端直接跳过**：定位是后端 + Agent 应用开发。
4. **优先级永远：项目 > 八股**。一个能讲透的项目 + 及格的八股，远胜满分八股 + 空白项目。
5. **每周日必做"合卷自测"**，防止假学会。
6. **Git 约定**：学习代码推自己的 fork，不 push 上游（也没有权限）。

---

## 六、与旧计划的关系

- 本计划接替 `学习计划.md`（8 周实习冲刺计划 v2，2026.7.23）中未完成的技术学习部分，目标从"快速拿实习 offer"调整为"打牢后端 + Agent 双能力并产出作品"。
- `open-source-learning/README.md` 中"4 周学完两个项目"的节奏由本计划的 9 周版本替代，重要约定（复现≠抄代码、延伸差异化、fork 提交）继续有效。
