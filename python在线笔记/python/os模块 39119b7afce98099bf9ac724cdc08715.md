# os模块

1. `os.environ`：一个字典对象，每个进程都有一个os.environ字典对象，涵盖当前进程的所有环境变量（常使用get( )函数取相关环境变量）
2. `os.getenv()` :标准库os模块提供的函数方法，用于读取操作系统的环境变量，相当于上面的`os.environ + get()` 。
    
    有函数签名： `os.getenv(key,default=None)` key为环境变量名、default为变量不存在时的返回值，默认为None。